export enum AlgsModes {
    ENCRYPT = 'Encrypt',
    DECRYPT = 'Decrypt'
}

export enum AlgorithmCommands {
    ENCRYPT = 'encrypt',
    DECRYPT = 'decrypt',
    CODE = 'encode',
    DECODE = 'decode',
    HASH = 'hash',
    GEN_KEYS = 'keys',
    GET_BLOCKS = 'blocks'
}
